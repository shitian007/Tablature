package com.tabs.tablature.framework.base;

public class Note extends InteractiveSpriteBase {

    public Note(String assetPath, float initialX, float initialY) {
        super(assetPath, initialX, initialY);
    }

}
